$(function () {
    //日期范围
    laydate.render({
        elem: '#onDate',
        range: '至',
        max:0
    });
    laydate.render({
        elem: '#cardDate',
        range: '至',
        max:0
    });
    
    function doQuery() {
        
    }


})
